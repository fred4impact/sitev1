DJANGO_SECRET_KEY=change-me-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
CORS_ALLOWED_ORIGINS=http://localhost:3000
FRONTEND_URL=http://localhost:3000

# Defaults to sqlite if unset. For Postgres:
# DATABASE_URL=postgres://user:password@localhost:5432/songroom

# Leave blank in dev: orders auto-complete without payment, emails are logged instead of sent
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=
RESEND_API_KEY=
DEFAULT_FROM_EMAIL=Jazz91 <onboarding@resend.dev>
CONTACT_EMAIL=info@jazz91lounge.art

# Leave blank in dev: media falls back to local filesystem storage.
# Set all three in production (Railway) so uploads persist across redeploys.
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
